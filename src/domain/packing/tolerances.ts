/**
 * Единый допуск по высоте (мм) для упаковки и валидации:
 * группировка слоёв, нормализация высоты, плоскости опоры в движке,
 * допустимый зазор по Z при проверке опоры (`hasSupport`).
 */
export const HEIGHT_TOLERANCE_MM = 20;
