---
topic: Модель документа возврата товаров
goal: Выбрать понятную модель возвратов и решить, нужен ли отдельный Return либо единый двунаправленный документ движения
mode: partner
status: complete
updated: 2026-09-21T18:39
---

- (direction by user) Возвраты сейчас привязаны к отгрузкам; проверить правильность связи и идею единого документа, работающего в обе стороны подобно Reservation
- (decision by coach) Creative Partner; порядок техник: Job to Be Done, TRIZ Contradiction, Morphological Analysis, Failure Analysis
- (technique by coach) started Job to Be Done
- (insight by coach) Текущий Return ссылается на Shipment в заголовке и на ShipmentLine в строках; это ограничивает суммарный возврат отгруженным количеством и выводит исходный заказ, склад и товар
- (insight by coach) Проведение возврата переносит товар из места customer_order обратно на склад исходной отгрузки и сохраняет owner=order; возврат не делает товар свободным
- (idea by coach) Связь с отгрузкой нужна как provenance и лимит, но это не требует отдельного типа документа: единая сущность CustomerMovement может иметь direction=outbound|inbound и inbound ссылаться на исходящий документ
- (question by coach) Какую работу должен выполнять возврат: только физически вернуть ранее отгруженный товар на исходный склад или ещё решать приёмку, состояние товара, другой склад, обмен и возврат денег?
- (decision by user) Возврат выполняет только отменяющее физическое действие: вернуть ранее отгруженный товар; приёмка, состояние, другой склад, обмен и деньги вне сценария
- (insight by coach) При таком JTBD Shipment и Return имеют один предмет и противоположный эффект; отдельная бизнес-сущность Return несёт мало самостоятельного смысла
- (idea by coach) Проведённую отгрузку не менять и не переводить назад в draft: создавать компенсирующий документ того же типа с direction=return и reverses_document_id на исходную отгрузку
- (question by coach) Отмена должна быть только полной один-к-одному или можно возвращать часть строк и создавать несколько возвратных движений на одну отгрузку?
- (question by user) Поставлена под сомнение необходимость reverses_document_id
- (insight by coach) reverses_document_id нужен только для явной связи и ограничения обратного количества; если каждая обратная строка хранит reverses_line_id, исходный документ однозначно выводится и header-ссылка дублирует данные
- (idea by coach) Минимальная единая модель: направление в заголовке, product+qty в исходящих строках, source_line_id только в возвратных строках; заказ, склад и исходный документ возврата выводятся через source line
- (question by user) Поставлена под сомнение необходимость source_line_id
- (insight by coach) source_line_id нужен только для атрибуции возврата конкретной строке и ограничения по ней; при модели обратного движения источником истины может быть текущий shipped-баланс товара в месте customer_order
- (idea by coach) Полностью симметричная модель без ссылок на исходную отгрузку: единый документ order+warehouse+direction и строки product+qty; outbound двигает warehouse→customer_order, inbound двигает customer_order→warehouse, лимит inbound задаёт фактический остаток в customer_order
- (question by coach) Возвращённый товар обязан идти на склад исходной отгрузки или склад возврата можно выбирать?
- (decision by user) Склад возврата можно выбирать; возврат не обязан идти на склад исходной отгрузки
- (insight by coach) Связи возврата с исходными Shipment и ShipmentLine не нужны ни для выбора склада, ни для лимита; достаточно заказа, выбранного склада и текущего shipped-баланса по товару
- (technique by coach) started TRIZ Contradiction
- (insight by coach) Противоречие: единая сущность устраняет дублирование модели, но пользователю отгрузка и возврат могут оставаться разными намерениями, действиями и списками
- (question by coach) Должны ли в интерфейсе остаться отдельные разделы Отгрузки и Возвраты, даже если в данных это одна сущность?
- (decision by user) В интерфейсе не нужны отдельные разделы Отгрузки и Возвраты; при создании и просмотре направление должно быть сразу очевидно
- (idea by coach) Разрешить TRIZ-противоречие разделением слоёв: одна сущность и один список, но direction управляет названием, глаголом кнопки, цветом/иконкой и явной схемой Откуда → Куда в форме и карточке
- (idea by coach) Нейтральный раздел Движения по заказам; в документе крупный тип Отгрузка или Возврат, а форма начинается с выбора направления Клиенту или От клиента
- (question by coach) Какое нейтральное имя дать единой сущности и разделу, чтобы оно естественно включало и отгрузку, и возврат?
- (insight by user) Название Движения по заказам будет путаться с перемещениями и другими товарными действиями
- (decision by user) Единый раздел называется Отгрузки и возвраты
- (insight by coach) Пользовательское составное имя может быть точнее нейтрального зонтичного термина; внутреннее имя таблицы и типа не обязано отображаться в UI
- (technique by coach) started Morphological Analysis
- (question by coach) В модели хранить явное operation=shipment|return или хранить from/to и вычислять тип документа по направлению?
- (decision by user) Тип единого документа хранится явно: operation=shipment|return
- (idea by coach) Количество остаётся положительным; operation определяет проводку: shipment warehouse→customer_order, return customer_order→warehouse
- (question by coach) Нумерация должна оставаться различимой по направлениям с префиксами отгрузки и возврата или стать общей последовательностью единого документа?
- (direction by user) Название поля направления должно совпадать с термином документа Reservation; нумерация единой сущности общая
- (insight by coach) В актуальной owner-модели Reservation поле operation удалено; направление вычисляется helper reservationDirection и в TS/UI называется direction. Для терминологической симметрии новое явное поле следует назвать direction
- (decision by coach) Единый документ хранит явное direction=shipment|return и использует общую последовательность номеров
- (question by coach) Проведённую отгрузку запрещаем отменять сменой статуса и разрешаем компенсировать только новым документом с direction=return?
- (question by user) Запрошено пояснение, действительно ли направление Reservation вычисляется
- (insight by coach) Reservation не хранит direction: toOwner=Free означает release; destination задан и все fromOwner=Free означает reserve; остальные переходы означают reassign
- (decision by user) Направление единого документа Отгрузки и возвраты вычисляется из from/to, как у Reservation; отдельное поле direction не хранится
- (idea by coach) Заголовок хранит from_location_type/id и to_location_type/id; допустимы только warehouse→customer_order и customer_order→warehouse; строки содержат product_id и положительное quantity
- (insight by coach) Без связи с исходной отгрузкой частичные и повторные возвраты естественно ограничиваются текущим балансом товара в customer_order, а отгрузки — reserved-балансом заказа на исходном складе
- (question by coach) Разрешаем любое количество частичных отгрузок и возвратов, пока текущего остатка в исходной точке достаточно?
- (decision by user) Допускается любое число частичных отгрузок и возвратов; проведение разрешено при достаточном текущем остатке в исходной точке
- (technique by coach) started Failure Analysis
- (insight by coach) Критичный риск — изменение или отмена проведённого документа разрушит неизменяемую историю и может сделать последующие движения необъяснимыми
- (idea by coach) Черновик можно отменять или редактировать; проведённый документ неизменяем, а его эффект компенсируется новым документом противоположного направления
- (question by coach) Фиксируем правило: проведённые документы нельзя отменять статусом, только компенсировать новым документом обратного направления?
- (decision by user) Черновики для отгрузок и возвратов не нужны
- (idea by coach) Создание и проведение должны быть одной атомарной операцией: либо документ и проводки созданы вместе, либо не создано ничего
- (insight by coach) Если все документы сразу проведены и не отменяются статусом, поле status не выражает изменяемого состояния и может быть удалено
- (question by coach) Убираем status полностью и считаем сам факт существования документа признаком проведённой операции?
- (decision by user) Поле status удаляется; существование документа означает, что операция уже проведена
- (insight by coach) После возврата нужно определить owner остатка: отмена физической отгрузки может восстановить order-owned reserved на выбранном складе либо освободить товар; это разные бизнес-эффекты
- (idea by coach) Как строго отменяющее действие возврат должен восстановить состояние до отгрузки: товар на выбранном складе остаётся зарезервированным за тем же заказом и доступен для повторной отгрузки
- (question by coach) После возврата товар остаётся зарезервированным за заказом на выбранном складе, а не становится свободным?
- (insight by user) При возврате логичнее делать товар свободным; модель должна позволять по строкам выбирать, что сделать с товаром, аналогично Reservation
- (insight by coach) Это делает возврат не строгим обратным движением: он одновременно переносит товар order→warehouse и может менять owner с order на Free либо другое назначение
- (insight by coach) У текущей Reservation destination owner задан в заголовке, а в каждой строке выбирается source owner; для возврата естественная зеркальная модель — склад назначения в заголовке и destination owner в строке
- (question by coach) На строке возврата выбирать назначение остатка: Свободно, текущий заказ или регион?
- (direction by user) Приостановить проектирование возврата и внимательно исследовать текущий процесс Reservation: назначение в заголовке, источник по строкам, найти проблемы
- (insight by coach) Текущий Reservation не позволяет выбирать действие каждой строки: место и destination owner общие в заголовке, строка выбирает только source owner, товар и количество
- (insight by coach) Смешанный документ Free→Order плюс Region→Order целиком получает direction=reassign; резервная строка теряется под общей меткой Переназначение
- (insight by coach) Связанные документы и покрытие заказа определяют касание по destination или любому source, после чего могут приписать затронутому заказу все строки смешанного Reservation
- (insight by coach) UX-проблемы Reservation: Назначение путается с местом; снятие стартует в запрещённом Free→Free; выбор source owner зависит от ещё не выбранного товара; продукт назначения не фильтруется по составу заказа; контекстные presets можно изменить
- (insight by coach) Черновик Reservation слабо редактируем и не удаляется из UI; create+lines+post выполняются отдельными запросами, поэтому сбой оставляет черновик, retry создаёт дубль
- (insight by coach) Posting Reservation и store_move не блокируют документ или bucket; конкурентное проведение может повторить проводки или увести остаток ниже нуля
- (insight by coach) Коррекция предыдущего чтения: актуальный store_post_return переносит customer_order/order → выбранный текущей моделью исходный warehouse/Free; возврат уже освобождает товар, а не сохраняет owner заказа
- (idea by coach) Если действие действительно выбирается по строкам, строка должна быть самодостаточной: product, qty, fromOwner, toOwner; место остаётся в заголовке для Reservation, а у физического движения в заголовке остаются fromPlace и toPlace
- (question by coach) Переделываем сам Reservation на fromOwner→toOwner в каждой строке или оставляем его одноназначенным, а построчный выбор применяем только к возврату?
- (insight by user) Мультисточник Reservation нужен осознанно: один документ бронирует под заказ товар одновременно из Free и региональных резервов, чтобы не создавать несколько документов
- (decision by coach) Для Reservation сохраняется destination owner в заголовке и source owner в строках; перенос обоих owner в строку разрушил бы смысл одного документа как сборки одного назначения из нескольких источников
- (insight by coach) Проблема не в мультисточнике, а в классификации: Free→Order и Region→Order в одном документе имеют единое намерение забронировать под заказ, но текущий helper называет весь документ Переназначением
- (question by coach) Для назначения Заказ разрешены источники только Free и Region или также другой Order?
- (decision by user) Для возврата склад назначения находится в заголовке, а в каждой строке выбирается назначение остатка: Free, текущий заказ или регион
- (idea by coach) Направление документа определяется только физическими endpoints и не зависит от line owner: warehouse→customer_order = отгрузка, customer_order→warehouse = возврат
- (idea by coach) При отгрузке owner строки фиксирован текущим заказом на обеих сторонах; при возврате source owner всегда текущий заказ, а destination owner выбирается по строке: Free, тот же Order или Region
- (question by coach) Можно ли один возвращаемый товар разбить на несколько строк с разными назначениями, например часть Free, часть текущему заказу, часть региону?
- (decision by user) Один возвращаемый товар можно разбивать на несколько строк с разными destination owners
- (idea by coach) Уникальность возвратной строки задаётся document + product + destination owner; сумма всех строк товара ограничена текущим shipped-балансом этого товара у заказа
- (question by coach) При отгрузке по-прежнему разрешать только товар, уже зарезервированный за текущим заказом, или также выбирать по строкам источник Free/Region и резервировать одновременно с отгрузкой?
- (decision by user) Обычная отгрузка имеет один физический источник-склад и списывает только резерв текущего заказа на этом складе
- (idea by user) По явному желанию пользователя интерфейс может выполнить составное действие: сначала создать Reservation из выбранных Free/Region источников под заказ, затем провести отгрузку
- (idea by coach) Составное действие должно показывать предварительный итог: какие количества и из каких owners будут сначала зарезервированы, затем какой итоговый объём будет отгружен; созданные Reservation остаются отдельными видимыми документами
- (question by coach) Если резервирование удалось, а отгрузка не прошла, откатываем весь составной сценарий атомарно или оставляем созданный резерв?
- (decision by user) В составном Reserve then Ship сценарии, если отгрузка не прошла после успешного Reservation, созданный резерв сохраняется
- (insight by coach) Сценарий намеренно неатомарный на уровне бизнес-документов: UI должен отдельно подтвердить успех Reservation и ошибку Shipment, показать оставшийся резерв и предложить повторить только отгрузку
- (question by coach) При возврате назначение Region может быть любым выбранным регионом или только регионом исходного заказа?
- (decision by user) Возвращённый товар можно назначить любому выбранному региону, не только региону исходного заказа
- (insight by coach) Прямое назначение возврата другому заказу не требуется: строка допускает только Free, текущий Order или любой Region; перенос другому заказу выполняется отдельным Reservation
- (question by coach) На общей странице создания сделать две явные кнопки Отгрузить и Принять возврат или одну кнопку Новый документ с первым выбором направления?
- (decision by user) На общей странице одна кнопка Новый документ; направление выбирается внутри сценария создания
- (idea by coach) Первый экран формы показывает две крупные операции Отгрузить и Принять возврат; после выбора меняются заголовок, схема Откуда→Куда, поля строк и главный глагол
- (question by coach) После первого выбора направление фиксируется, а смена возможна только с очисткой формы?
- (decision by user) После выбора Отгрузка или Возврат направление фиксируется; изменить его можно только вернувшись к первому шагу с очисткой формы
- (idea by coach) Общая форма после первого шага всегда выбирает заказ и склад; направление лишь меняет роли склада и заказа как Откуда/Куда, а для возврата добавляет destination owner по строкам
- (question by coach) В общем списке показывать все документы вместе с быстрыми фильтрами Все, Отгрузки, Возвраты?
- (decision by user) Общий список показывает все документы вместе и имеет фильтры Все, Отгрузки, Возвраты
- (technique by coach) started PMI convergence for the unified Shipments and Returns model
- (insight by coach) PMI Plus: одна сущность, список, форма и нумерация; нет provenance-ссылок; любой склад возврата; частичные повторяемые движения; гибкое построчное назначение возврата
- (insight by coach) PMI Minus: теряется атрибуция возврата конкретной отгрузке; правила строк асимметричны по направлению; один товар может повторяться по destination owner; Reserve then Ship требует явного восстановления после частичного успеха
- (insight by coach) PMI Interesting: direction не хранится, но UI выбирает его первым и материализует допустимую пару endpoints; существование документа равно проведению; destination owner строки описывает состояние после физического движения
- (decision by coach) Рекомендованная модель прошла PMI: document хранит fromPlace→toPlace, line хранит product+qty+destination owner, направление вычисляется и ограничивается двумя маршрутами; создание атомарно и без status
- (decision by user) Итоговая единая модель Отгрузок и возвратов принята после PMI
- (insight by user) Главная ценность итоговой модели: интерфейс единый, но намерение всегда явно видно
- (insight by coach) Единая модель не требует обезличенного UI: первый фиксированный выбор намерения управляет маршрутом, полями, текстами и действиями, а направление остаётся вычисляемым инвариантом данных
- (insight by coach) Построчные назначения возврата не размывают намерение документа, потому что намерение задаёт физический маршрут заказа и склада, а owner строки описывает только дальнейшую судьбу остатка
- (insight by coach) Reserve then Ship остаётся честной композицией двух документов: единый UI упрощает действие, но не скрывает отдельно созданный Reservation и частичный успех
