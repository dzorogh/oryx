import type { ReactNode } from "react";

type LogisticsRedirectLayoutProps = {
  children: ReactNode;
};

const LogisticsRedirectLayout = ({ children }: LogisticsRedirectLayoutProps) => children;

export default LogisticsRedirectLayout;
